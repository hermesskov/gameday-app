import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // App requires VBL API connectivity and auth — full widget tests
    // are deferred to manual testing per spec.
    expect(true, isTrue);
  });
}
